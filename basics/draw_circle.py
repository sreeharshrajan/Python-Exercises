# Turtle is a Python feature like a drawing board, which lets us command a turtle to draw all over it!
import turtle

t = turtle.Turtle()
t.fillcolor("red")
t.begin_fill()
radius = 50
t.circle(radius)
t.end_fill()
